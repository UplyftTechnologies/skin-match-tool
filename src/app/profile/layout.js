export const metadata = {
  alternates: {
    canonical: "/profile",
  },
  robots: {
    index: false,
    follow: true,
  },
};

export default function ProfileLayout({ children }) {
  return children;
}

export const metadata = {
  alternates: {
    canonical: "/wishlist",
  },
  robots: {
    index: false,
    follow: true,
  },
};

export default function WishlistLayout({ children }) {
  return children;
}
